name: myworkflow

on:
  push: 
    branches:
      - main

jobs:
  ex-job:
    runs-on: ubuntu-latest


    steps:
      - name: Print welcome message
        run: echo " Welcome to the workflow!"
      - name: Checkout Reository
        uses: actions/checkout@v6.0.3
      - name: List reository files
        run: ls -la
